{
  "name": "panos-mcp",
  "version": "1.3.29",
  "description": "MCP server for Palo Alto Networks PanOS firewalls",
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "main": "dist/index.js",
  "bin": {
    "panos-mcp": "dist/index.js",
    "panos-keygen": "dist/cli/keygen.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "keygen": "node dist/cli/keygen.js",
    "test": "vitest run",
    "bundle": "node scripts/bundle.js",
    "pack:extension": "npm run build && npm run bundle && node scripts/pack-extension.js",
    "release": "node scripts/release.js",
    "prepare": "npm run build"
  },
  "keywords": [
    "mcp",
    "panos",
    "palo-alto",
    "firewall"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@napi-rs/keyring": "^1.2.0",
    "fast-xml-parser": "^5.10.1",
    "socks": "^2.8.3",
    "undici": "^8.10.0",
    "zod": "^4.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "esbuild": "^0.28.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
